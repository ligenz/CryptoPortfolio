<h1>These are my favorite cryptos</h1>

<% @coins %>

<% for x in @coins %>
    <% for coin in @coins_arr %>
        <% if coin == x["symbol"] %>
            <p><%= x["name"] %>: $<%= x["price_usd"] %></p>
            <p>Rank: <%= x["rank"] %></p>
        <% end %>
    <% end %>
<% end %>